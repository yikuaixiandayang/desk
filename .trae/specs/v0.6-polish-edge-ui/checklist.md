# Checklist

## 问题1 趴卧贴合任务栏
- [ ] 精灵图帧 8/9 内容底部空白 ≤2px（像素扫描验证），行 0 素材未变（托盘图标不受影响）
- [ ] resources/ 与渲染 assets/ 两份精灵图同步更新
- [ ] 帧 8/9 交替动画身体底边不跳动（内容底边一致）
- [ ] 趴卧贴合间距可在设置面板调节（0~20px）并持久化，`squatAnchorY` 实际使用该值
- [ ] GUI 实测：拖到任务栏松手后宠物身体紧贴任务栏顶沿，无明显空隙

## 问题2 右键菜单位置
- [ ] 宠物在屏幕中部右键：菜单出现在宠物右侧，间隙 ~8px，不覆盖宠物
- [ ] 宠物贴右缘右键：菜单自动放左侧；极窄视口不越界（纯函数单测覆盖三分支）
- [ ] 垂直方向对齐宠物身体中部，菜单完整可见

## 问题3 上/左/右边缘动作与台词
- [ ] edgeSnap 默认开启（新配置默认 true；存量显式 false 尊重）
- [ ] 漫步到达左/右边缘触发侧卧 + 方向台词（与拖拽行为一致）
- [ ] 上边缘触发延迟探头 + 探头台词（新增台词池，按方向+情绪）
- [ ] 拖拽到上/左/右边缘仍正常触发对应动作（无回归）
- [ ] GUI 实测三个方向动作与台词均出现

## 问题4 AI 台词
- [ ] AMBIENT_PROMPT 单测：含情绪/时段/等级/饱食度上下文字段，要求 ≤40 字
- [ ] 主进程 `chat:ambient` IPC + preload 暴露 + 失败返回 ok:false（不抛错）
- [ ] moodTick 主动搭话：开启时走 GLM，失败/关闭降级本地台词池；限频与防打扰不变
- [ ] 设置面板有「AI 主动搭话」开关与「立即搭话」按钮，配置持久化
- [ ] GUI 实测「立即搭话」：GLM 正常时返回生成台词；断网/接口异常时优雅降级

## 问题5 设置面板不遮挡宠物
- [ ] bubble/menu/chatbar/toolbar 四元素 GSAP xPercent/yPercent 修复落地
- [ ] 打开设置：面板底边在宠物头顶上方 ~12px，弹出动画前后位置一致，不与宠物重叠
- [ ] 气泡/聊天条/工具条同样不压宠物头部（show/hide 动画后位置正确）
- [ ] GUI 截图验证通过

## 问题6 探头主体可见
- [ ] `peekContentMetrics` 常量与素材实测内容边界一致
- [ ] 新偏移算法单测：三方向峰值角色可见比例 ≥60%（默认 0.3 时 ≈70%）
- [ ] 素材裁切边与屏幕边缘对齐（左探头的裁切边贴屏幕左缘）
- [ ] 「演示探头」：宠物先滑到边缘停靠位再探头，无全屏漂移；三方向轮换正常
- [ ] 探出幅度滑条语义为「探出比例」（0.05~0.6，默认 0.3），存量值 clamp 生效
- [ ] GUI 实测三方向探头：身体大部分可见，能看清动作

## 问题7 面板化配置
- [ ] 新增设置项：AI 主动搭话开关 / 立即搭话 / 趴卧贴合间距 / 探出幅度（语义更新），全部面板可调并持久化
- [ ] 所有新配置走现有 `onPetConfig` 通道，重启后恢复

## 回归与工程验证
- [ ] `npm run typecheck` 0 错误
- [ ] `npm test` 全部通过（含新增用例）
- [ ] `npm run build` 成功
- [ ] `npm run dev` GUI 启动无控制台报错，`[pet] ready` 日志出现
- [ ] 既有功能（拖拽/聊天/TTS/番茄钟/右键菜单全项）无回归
- [ ] 验证过程发现的问题已就地修复并复验
