# 桌面宠物应用 Spec

## Why
用户希望拥有一只常驻桌面的虚拟宠物，通过好感度、情绪、动画与语音/文字交互，陪伴用户工作并提供健康提醒。当前工作目录无任何相关实现，需要从零搭建一个基于 Electron 的桌面宠物应用，并对接提供的 GLM-5.2 OpenAI 兼容接口。

## 实现计划（阶段与里程碑）
- **阶段 0：端口验证（前置）**
  - 目标：验证 `http://172.22.40.153:8642` 可达且 `/v1/chat/completions` 可用提供的 API Key 调通 GLM-5.2。
  - 产出：连通性报告（可达性、模型响应样例、错误码）。若不可达，先以 mock 完成对接骨架并标注待验证，不阻塞其余阶段。
  - 验收：可发起一次成功（或明确失败）的 chat completions 请求并记录响应。
- **阶段 1：项目骨架与窗口**
  - Electron + TS + Vite，透明置顶窗 + 点击穿透 + 预加载安全桥。
  - 验收：应用启动显示透明窗口，非宠物区域点击穿透。
- **阶段 2：图形化宠物与移动**
  - Canvas 渲染 + 帧动画 + 自主移动 + 拖拽。
  - 验收：宠物自主跑动不越界，可拖拽。
- **阶段 3：情绪与好感度**
  - 好感度（0-100）持久化 + 情绪计算 + 多姿势动画（默认/开心/生气垂手/撒娇）。
  - 验收：互动改变好感度，事件驱动情绪并切换姿势。
- **阶段 4：交互与对话**
  - 中文语音/文字输入 + TTS + GLM-5.2 对接 + 秘书式提示 + 降级。
  - 验收：中文输入可获取秘书式回复，语音可输入并语音回应，接口异常降级。
- **阶段 5：健康提醒**
  - 使用时长监控 + 久坐/喝水提醒（可配置阈值）。
  - 验收：提醒按时触发并由宠物展示。
- **阶段 6：持久化与集成验收**
  - 统一本地存储 + 重启恢复 + 全链路验收。
  - 验收：重启后状态/历史恢复，全部 checklist 通过。

## 技术选型
| 层面 | 选型 | 说明 |
| --- | --- | --- |
| 桌面框架 | Electron | 跨平台、支持透明置顶窗与系统集成 |
| 语言 | TypeScript | 类型安全，便于维护 |
| 构建/打包 | Vite + electron-builder | Vite 负责渲染进程热更新，electron-builder 产出安装包 |
| 宠物渲染 | Canvas 2D | 帧动画性能好，便于绘制情绪姿势；SVG 作为静态资源备选 |
| 状态管理 | 渲染进程内存 + 本地存储 | 好感度/情绪/使用时长等状态由渲染进程统一管理 |
| 本地存储 | electron-store | JSON 持久化，轻量、跨平台 |
| 语音识别(ASR) | Web Speech API（首选） | 浏览器原生、免密钥；离线方案作为降级（详见补充信息） |
| 语音合成(TTS) | Web Speech API SpeechSynthesis | 中文语音输出，免额外依赖 |
| 对话接口 | GLM-5.2（OpenAI 兼容） | 见"对接配置" |
| 进程通信 | contextBridge + ipcRenderer | 安全暴露能力至渲染进程 |

## 对接配置
- API Base URL：`http://172.22.40.153:8642`
- 接口：`POST /v1/chat/completions`
- 模型：`GLM-5.2`
- 协议：OpenAI 兼容
- API Key：`hermes_sk_7f3a9c2e1b8d4a6e5c0f2a9d3e7b1c4f8a6d2e9b`
- 鉴权头：`Authorization: Bearer <API Key>`
- 请求/响应：遵循 OpenAI Chat Completions 结构（`messages`、`temperature` 等）
- 超时与重试：默认 30s 超时，失败降级为本地预设回复（详见"接口异常"）
- 配置位置：集中存放于 `src/config/api.ts`，API Key 通过本地配置文件/环境变量注入，避免硬编码入库

## What Changes
- 新建 Electron + TypeScript 项目骨架（主进程、渲染进程、预加载脚本）
- 实现透明置顶窗口与桌面宠物渲染层（Canvas 图形化动画）
- 实现桌宠自主移动与多姿势/情绪动画系统
- 实现好感度系统（数值持久化、随互动变化）
- 实现情绪系统（开心/生气/撒娇等，受好感度与事件驱动）
- 实现交互系统：中文语音输入（Web Speech API / 离线方案）、文字输入、语音/文字回应
- 实现健康提醒功能：电脑使用时长监控、休息/喝水提醒
- 对接 GLM-5.2 OpenAI 兼容接口（POST /v1/chat/completions）用于对话回复
- 架构决策：采用方案 A（直接使用 API，数据保留本地），理由见下文

### 架构决策建议
**推荐方案 A：直接使用 API，数据保留在本地。**

理由：
1. 好感度、情绪、使用时长等均为本地状态，本地处理延迟低且无需额外服务。
2. 方案 B 引入 agent 服务端会带来部署/维护成本与隐私顾虑，当前需求未体现多端数据共享必要性。
3. GLM 接口为无状态对话，本地保存会话历史即可完成上下文管理，无需 agent 中转。
4. 后续若需数据共享，可在方案 A 基础上增量增加同步层，演进成本可控。

方案 A vs 方案 B 对比：

| 维度 | 方案 A（直接 API，本地数据） | 方案 B（agent，数据共享） |
| --- | --- | --- |
| 部署复杂度 | 低，仅客户端 | 高，需服务端 |
| 延迟 | 低 | 略高（多一跳） |
| 多端共享 | 不支持 | 支持 |
| 隐私 | 数据留在本地 | 数据上云 |
| 维护成本 | 低 | 高 |
| 演进路径 | 可增量加同步层 | 已含服务端 |

若用户后续明确需要多设备共享或服务端集中决策，再切换为方案 B。

## 所需补充信息
实现过程中需用户确认/提供以下信息：

**通用**
1. **宠物形象**：是否提供美术素材（精灵图/PNG 序列）？若无，将使用代码绘制的简笔图形占位，后续可替换。
2. **提醒阈值**：默认久坐 45 分钟、喝水每 60 分钟，是否调整？
3. **打包平台**：仅需 Windows，还是需 macOS/Linux？
4. **开机自启**：是否需要支持开机启动？

**语音方案**
5. **语音识别**：Web Speech API 在 Electron 中依赖在线服务且识别质量受网络影响；是否接受在线识别？若需离线中文 ASR，需额外引入模型（如 Vosk/Whisper.cpp），会增大体积，请确认偏好。
6. **语音输出**：使用系统自带 SpeechSynthesis 中文语音；若无中文语音包，是否接受安装/降级为纯文字回复？

**API 对接**
7. **API 端点可达性**：`http://172.22.40.153:8642` 为内网地址，需确认开发机可访问；阶段 0 会先做连通性探测。
8. **示例代码框架**：需求提到"Electron 调用示例代码框架已提供"，但当前未见到代码；请提供该框架或确认是否需要我方生成对接示例。
9. **用量与限流**：GLM-5.2 是否有 QPS/Token 限流？影响会话历史截断策略。
10. **会话历史长度**：默认保留最近 20 轮对话以控制上下文长度，是否调整？

**架构选择（方案 A vs 方案 B）**
11. **数据共享需求**：确认无多端共享需求以锁定方案 A；若未来需要，再引入同步层。
12. **若改用方案 B（agent）所需补充**：
    - agent 服务端地址与协议（是否复用同一 GLM 端点？）
    - 数据共享范围（仅会话？还是含好感度/情绪/使用时长？）
    - 鉴权方式与用户标识（多用户隔离方案）
    - 部署环境（谁负责运维 agent 服务？）
    - 离线/断网时客户端回退策略
    - 隐私与合规要求（数据存储地域、保留周期）

## Impact
- Affected code: 新建项目，无现有代码受影响
  - `src/main/` Electron 主进程
  - `src/renderer/` 渲染进程（宠物 UI/动画）
  - `src/preload/` 预加载桥接
  - `src/core/` 好感度/情绪/提醒/移动逻辑
  - `src/services/` API 对接、语音、存储
  - `src/config/` API Key、模型、阈值等配置
- 外部依赖：Electron、TypeScript、Vite、electron-builder、electron-store、GLM-5.2 API

## ADDED Requirements

### Requirement: 透明置顶桌面窗口
系统 SHALL 创建透明、无边框、置顶的 Electron 窗口，宠物图形悬浮于桌面，点击穿透非宠物区域。

#### Scenario: 启动应用
- **WHEN** 用户启动应用
- **THEN** 桌面出现透明置顶窗口，显示默认姿势的宠物，且不阻挡桌面其他操作

### Requirement: 图形化宠物渲染
系统 SHALL 使用 Canvas（或 SVG 作为静态资源）渲染宠物图形（非纯文字），支持多帧动画。

#### Scenario: 渲染宠物
- **WHEN** 应用运行
- **THEN** 桌面显示图形化宠物，具备平滑动画

### Requirement: 自主移动
系统 SHALL 使宠物在桌面范围内自主随机移动（"能自己跑"），移动路径不超出屏幕边界。

#### Scenario: 自主移动
- **WHEN** 宠物处于空闲且未被拖拽
- **THEN** 宠物每隔随机时间自主移动到新位置

#### Scenario: 拖拽中断
- **WHEN** 用户拖拽宠物
- **THEN** 自主移动暂停，释放后恢复

### Requirement: 多姿势情绪动画
系统 SHALL 提供多种姿势动画：默认、开心、生气（垂手）、撒娇等，由情绪状态驱动切换。

#### Scenario: 生气表现
- **WHEN** 功能未实现或用户长时间未响应
- **THEN** 宠物切换为生气垂手姿势

#### Scenario: 开心表现
- **WHEN** 好感度提升或收到积极互动
- **THEN** 宠物切换为开心姿势动画

### Requirement: 好感度系统
系统 SHALL 维护 [0, 100] 范围的好感度数值，持久化到本地，随互动增减。

#### Scenario: 互动增加好感度
- **WHEN** 用户进行语音/文字互动或抚摸
- **THEN** 好感度按规则增加

#### Scenario: 冷落降低好感度
- **WHEN** 用户长时间无互动
- **THEN** 好感度缓慢下降

### Requirement: 情绪系统
系统 SHALL 基于好感度与事件计算当前情绪（开心/生气/撒娇/平静），并影响动画与回复语气。

#### Scenario: 功能未实现生气
- **WHEN** 用户触发尚未实现的功能
- **THEN** 宠物表现为生气情绪并提示

#### Scenario: 未响应生气
- **WHEN** 用户长时间未响应提醒
- **THEN** 宠物表现为生气情绪

### Requirement: 中文语音与文字交互
系统 SHALL 支持中文语音输入与文字输入，并能以语音或文字方式回应。

#### Scenario: 语音输入
- **WHEN** 用户点击麦克风并说话（中文）
- **THEN** 系统识别语音并生成回复

#### Scenario: 文字输入
- **WHEN** 用户在输入框输入中文
- **THEN** 系统生成回复并以语音或文字呈现

### Requirement: GLM-5.2 对话对接
系统 SHALL 调用 GLM-5.2 OpenAI 兼容接口（Base URL `http://172.22.40.153:8642`，`POST /v1/chat/completions`，模型 `GLM-5.2`，API Key 见配置）生成回复，并将宠物情绪/好感度作为系统提示注入上下文；API Key 集中于配置模块，不硬编码于业务代码。

#### Scenario: 调用成功
- **WHEN** 用户发送消息
- **THEN** 系统调用 GLM 接口返回回复并展示

#### Scenario: 接口异常
- **WHEN** 接口不可达或返回错误（含超时）
- **THEN** 系统降级为本地预设回复并提示

### Requirement: 健康提醒
系统 SHALL 监控电脑连续使用时长，触发休息、喝水等健康提醒；默认久坐阈值 45 分钟、喝水周期 60 分钟（可配置）。

#### Scenario: 久坐提醒
- **WHEN** 连续使用达到设定阈值
- **THEN** 宠物弹出休息提醒

#### Scenario: 喝水提醒
- **WHEN** 到达喝水提醒周期
- **THEN** 宠物提示喝水

### Requirement: 个人秘书交互体验
系统 SHALL 以个人秘书语气组织回复，结合情绪与好感度调整语气，提供陪伴与提醒。

#### Scenario: 秘书式回复
- **WHEN** 用户提问或互动
- **THEN** 宠物以秘书语气回复，语气随情绪变化

### Requirement: 本地数据持久化
系统 SHALL 将好感度、情绪、使用时长、会话历史等数据保存到本地，应用重启后恢复。

#### Scenario: 重启恢复
- **WHEN** 应用重启
- **THEN** 好感度、情绪及历史数据恢复
