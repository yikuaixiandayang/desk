# Checklist

## 项目骨架
- [x] Electron + TypeScript + Vite 项目可正常启动并显示透明置顶窗口（构建通过，GUI 运行日志 `[pet] ready`；点击穿透经 setIgnoreMouseEvents(forward) 动态切换）
- [x] 预加载脚本通过 contextBridge 安全暴露 IPC API，未启用 nodeIntegration（contextIsolation: true）

## 宠物渲染与移动
- [x] 宠物以图形化（Canvas 2D）形式渲染，具备帧动画（用户精灵图 8 帧姿势 + 程序化呼吸/走路/拖拽动画）
- [x] 宠物可在桌面范围内自主移动且不越界（clampToArea 单测：边界含余量）
- [x] 用户拖拽宠物时自主移动暂停，释放后恢复（movement 单测：drag 相位不自主移动）

## 情绪与动画
- [x] 支持默认/开心/生气（垂手）/撒娇等多种姿势动画并可正确切换（情绪→帧映射 + 平静帧微变体轮换）
- [x] 情绪影响动画与回复语气（系统提示四情绪语气映射，单测验证互不相同）

## 好感度
- [x] 好感度数值在 [0,100] 范围，随互动增减、冷落下降（单测：clamp/互动增量/冷却/冷落扣减）
- [x] 好感度数据持久化（electron-store），应用重启后可恢复（selftest 验证 store 路径与读写）

## 情绪系统
- [x] 情绪状态可由好感度与事件驱动（开心/生气/撒娇/平静）（单测：基线映射 + 临时情绪到期回落）
- [x] 功能未实现或用户未响应时宠物表现为生气（语音失败/GLM异常→生气；提醒2分钟未确认→生气+文案）

## 交互系统
- [x] 支持中文文字输入并发送（输入条 + Enter/按钮，随 GUI 启动验证）
- [x] 支持中文语音输入识别（Vosk 离线 WASR：vosk-browser + 主进程模型管理与本地分发；模型已预下载。注：Web Speech API 在 Electron 不可用，经用户确认改用 Vosk）
- [x] 支持语音合成（SpeechSynthesis）输出中文回复（运行期检测中文语音包，缺失时降级纯文字并提示）

## GLM-5.2 对接
- [x] API 配置集中于 `src/config/api.ts`，API Key 通过本地配置/环境变量注入，未硬编码于业务代码（config.local.json 已 gitignore）
- [x] 已对 `http://172.22.40.153:8642/v1/chat/completions` 做连通性探测并产出报告（docs/connectivity-report.md：HTTP 200 / 1.68s / 样例回复）
- [x] 若端点不可达，已以 mock 完成对接骨架并标注待验证，未阻塞其余阶段（不适用：端点可达；降级路径已用错误端点实测）
- [x] 已与用户确认"Electron 调用示例代码框架"是否提供，或确认由我方生成对接示例（用户未提供，由我方生成：src/main/services/glm.ts）
- [x] 可调用 GLM-5.2 接口（POST /v1/chat/completions，正确 API Key 与模型）生成回复（selftest 实调返回"自测成功"）
- [x] 对话上下文注入宠物情绪/好感度作为系统提示（buildSystemPrompt 单测）
- [x] 接口异常或超时（30s）时降级为本地预设回复并提示（错误端点实测优雅失败；渲染层【离线回复】前缀提示）

## 健康提醒
- [x] 可监控电脑连续使用时长（powerMonitor 系统级空闲检测，30s 轮询，活跃/休息判定）
- [x] 久坐达到阈值（默认 45 分钟，可配置）时弹出休息提醒（决策逻辑单测：触发/不触发/防重复轰炸/休息重置）
- [x] 到达周期（默认 60 分钟，可配置）时弹出喝水提醒（决策逻辑单测；阈值在 ⚙️ 菜单可改）

## 秘书式体验
- [x] 回复以个人秘书语气组织，语气随情绪/好感度变化（系统提示 + 单测）
- [x] 健康提醒以秘书式表达呈现（秘书式提醒文案库 + 确认/未响应文案）

## 持久化与恢复
- [x] 好感度、情绪、使用时长、会话历史、提醒配置均可本地持久化（electron-store 统一模块）
- [x] 应用重启后所有状态与历史数据恢复（启动 hydrate：状态/历史/宠物位置/提醒时间戳）

## 交付前确认
- [x] 已与用户确认 spec.md "所需补充信息" 中的事项（语音方案、素材、默认项；2026-08-16）
- [x] 若 API 端点不可达，已以 mock 完成对接骨架并标注待验证（不适用：端点可达）

## 交付物
- `npm run dev` 开发运行 / `npm run build` 构建 / `npm run selftest` 无 GUI 自测（实测通过）
- `npm test` 单元测试 19/19 通过
- 连通性报告：docs/connectivity-report.md
- 交互功能（拖拽/抚摸/语音对话/提醒弹出）为实机体验项，逻辑层均有测试证据

## v0.2 迭代新增（2026-08-16 六项反馈）
- [x] 声音：Edge 神经语音默认（音色/语速/音调/音量/试听），sapi 降级，自定义克隆端点（OpenAI 兼容 /v1/audio/speech）
- [x] 后台控制台：API 地址/模型/Key/模式热改 + 测试连接；记忆目录更换/打开/导出/导入；本地明文记忆副本（json+md）
- [x] 宠物状态控制：走动/静止一键切换（工具条+控制台，持久化）
- [x] 智能增强：长期记忆注入上下文、定时提醒（自然语言+重启补发）、主动搭话、待办清单、本地技能（报时/笑话/帮助）
- [x] 情绪扩充至 9 种，行为差异完整（移动/弹跳/粒子/语音参数/台词），单测覆盖
- [x] README 重写：能力介绍 + 声音克隆指南 + 12 项功能提案菜单
- [x] 验证：单测 32/32、selftest 10/10（含 GLM 真实调用与 Edge TTS 实合成）、GUI 启动正常

## v0.3 迭代新增（2026-08-17）
- [x] 番茄钟（专注免打扰/三选交互/跨重启/统计）
- [x] 程序合成环境音（雨/篝火/三色噪）
- [x] Open-Meteo 天气（免密钥实调通过，城市记忆，穿衣带伞建议）
- [x] 全局快捷键 Alt+Y 呼出 / Alt+J 剪贴板解读
- [x] 今日报告 + 每日计数统计（跨天清零）
- [x] 验证：单测 43/43、selftest 13/13（天气实调 北京 22.7℃）、GUI 启动正常

## v0.3.1 回归修复（2026-08-17）
- [x] 修复 typecheck：app.ts 缺 onPetScale 回调导致 tsc 报错（v0.3 单测未覆盖 tsc，回归未被发现）
- [x] 缩放真正生效：effectivePetHeight() 统一渲染/布局/碰撞/粒子/拖拽高度，控制台滑条改动即时反映并持久化
- [x] 验证：typecheck 通过、单测 43/43 通过

## v0.4 功能扩展（2026-08-17）
- [x] 右键菜单：悬停宠物右键弹出（聊天/喂食/摸摸头/走动/设置/退出），边界反弹，Esc/点外关闭
- [x] 双击拥抱：两次轻点→拥抱（好感+3、撒娇、心形粒子、台词）
- [x] 视线跟随：瞳孔追随全局鼠标（世界坐标绘制，facing 镜像处理）
- [x] 边缘吸附：拖到屏幕边缘自动坐下（sit 相位 + 蹲姿），拖拽解除
- [x] 养成喂养：NurtureState 持久化（exp/level/satiety/items），喂食扣道具+加饱食度+经验+升级，饱食度随时间衰减，<30 饿哭
- [x] 道具掉落：聊天/摸头互动随机掉落（限频 5 分钟、30% 概率）
- [x] 控制台养成 tab：等级/经验条/饱食度条/道具库存/喂食按钮
- [x] GLM 注入：ChatContext 带 level/satiety，system prompt 体现等级感与饥饿
- [x] 验证：typecheck 通过、单测 60/60、selftest 13/13 pass:true

## v0.5 重构（2026-08-17）
- [x] 移除视线跟随（删黑点瞳孔，精灵图原样）
- [x] 多显示器：窗口覆盖所有 display 并集，screenBounds 返回合并区域，display 变化动态调整
- [x] 好感度并入养成：删除 affection，level 取代（互动加经验升级），情绪基线/prompt/台词全改 level，旧存档迁移
- [x] 左键=精简工具条（聊天+语音+菜单），悬停不再自动弹，右键菜单保留完整
- [x] 控制台参数可调：掉落概率/饱食衰减/移动间隔/早报时间
- [x] 每日早报：配置时间到点播报待办+天气+鼓励，跨重启补发
- [x] 增强记忆：[偏好]/[身份]/[日程]/[习惯] 标签，对话主动提及
- [x] 验证：typecheck 通过、单测 56/56、selftest 13/13 pass:true
